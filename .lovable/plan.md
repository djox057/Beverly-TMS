This was a data question, not an implementation request. Results from the `orders` table (filtered by `canceled = true` and `pickup_datetime`):

- Jul 6–12, 2026: **179 canceled**
- Jun 29 – Jul 5, 2026: **182 canceled**

No code changes are proposed. Let me know if you'd like to slice this differently (by dispatcher, company, broker, etc.) or surface it in the UI.